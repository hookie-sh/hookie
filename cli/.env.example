# Clerk publishable key (required for CLI auth when building from source).
# Get it from your Clerk dashboard: https://dashboard.clerk.com
CLERK_PUBLISHABLE_KEY=

# Web app URL for login redirect (optional; defaults to https://hookie.sh in prod builds).
# HOOKIE_WEB_APP_URL=
